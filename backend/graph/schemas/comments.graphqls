type Comment {
  _id: ID
  reviewId: ID
  userId: String
  user: UserResponse
  text: String
  createdAt: Time
  updatedAt: Time
  likes: [ID]
  likesCount: Int!
  dislikes: [ID]
  dislikesCount: Int!
  userReaction: String
}

type CommentsPage {
  totalComments: Int!
  totalPages: Int!
  hasPreviousPage: Boolean!
  hasNextPage: Boolean!
  comments: [Comment!]!
}

extend type Query {
  commentsPage(reviewId: String!, pageSize: Int, page: Int!): CommentsPage!
}

extend type Mutation {
  addComment(reviewId: String!, text: String!): Comment!
}